{
  "name": "nus-site",
  "private": true,
  "version": "0.1.0",
  "description": "nus.dev — the site for nus (terminus), a terminal emulator that is also a browser.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --serve",
    "sync": "node scripts/sync-docs.mjs"
  },
  "engines": { "node": ">=18" }
}
